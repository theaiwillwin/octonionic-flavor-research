This retained marker makes the project-local Temp proxy directory concrete.
The v3 ad-hoc verifier points the persisted Temp-absence check here because
the real Windows Temp directory necessarily contains the verifier while it runs.
Real Temp is checked independently, excluding only that live verifier path.
